// Apex dashboard — client-side bootstrap.
//
// The theme toggle uses inline Alpine (see templates/partials/header.html).
// The dark-mode flash prevention runs inline in base.html <head>.
// Alpine + HTMX are loaded via CDN in base.html.
//
// Future feature-specific JS (charts, drag-and-drop, etc.) goes here.
